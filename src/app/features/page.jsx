import React from 'react';

const page = () => {
    return (
        <div>
            features
            features
            features
            features
            features
            features
            features
            features
        </div>
    );
};

export default page;
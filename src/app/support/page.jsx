import React from 'react';

const page = () => {
    return (
        <div>
            support
            support
            support
            support
            support
            support
            support
        </div>
    );
};

export default page;